# Dependency constraints for reproducible CI installs.
#
# Usage: pip install -e ".[dev]" -c constraints.txt
#
# This pins every package (including transitive dependencies) that
# `pip install -e ".[dev]"` resolves, captured from a verified working
# environment on 2026-09-06 (Python 3.12; the Python 3.10-only conditional
# dependencies exceptiongroup/tomli are pinned to their current releases).
# Update deliberately, never implicitly: regenerate with
# `pip freeze --exclude-editable` after an intentional upgrade and re-run the
# full test suite.

arrow==1.4.0
attrs==26.1.0
exceptiongroup==1.3.1
fqdn==1.5.1
idna==3.19
iniconfig==2.3.0
isoduration==20.11.0
jsonpointer==3.1.1
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
lark==1.3.1
packaging==26.3
pluggy==1.6.0
Pygments==2.21.0
pytest==9.1.1
python-dateutil==2.9.0.post0
referencing==0.37.0
rfc3339-validator==0.1.4
rfc3986-validator==0.1.1
rfc3987-syntax==1.1.0
rpds-py==2026.6.3
ruff==0.16.6
six==1.17.0
tomli==2.4.1
typing_extensions==4.16.0
tzdata==2026.3
uri-template==1.3.0
webcolors==25.10.0
